<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/RelativeLayout1"
    android:layout_width="@dimen/tabIndicatorWidth"
    android:layout_height="@dimen/tabIndicatorHeight"
    android:background="@drawable/tab_indicator_bg_light" >

    <LinearLayout
        android:id="@+id/wc"
        android:layout_width="match_parent"
        android:layout_height="@dimen/tabIndicatorHalfHeight"
        android:layout_marginBottom="5sp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="false"
        android:layout_alignParentTop="false"
        android:layout_centerHorizontal="true"
        android:layout_centerInParent="false"
        android:layout_gravity="center"
        android:gravity="bottom"
        android:orientation="vertical" >

        <ImageView
            android:id="@+id/image"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:src="@drawable/ic_tab_ic_tab_exit" />

        <TextView
            android:id="@+id/tabTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="Medium Text"
            android:textColor="@drawable/tab_text_color"
            android:textSize="9sp" />

    </LinearLayout>

</RelativeLayout>
